import { products } from "../database"

const Product = () => {
  return (
    <div>
      {
        products.map(product => {
          product.
        })
      }
    </div>
  )
}

export default Product